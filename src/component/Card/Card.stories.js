import React from 'react'
import Card from './Card'

const story = {
    title : 'UInator/Card',
    component : Card
}

export default story

const Template = (args) => <Card></Card>

export const Default = Template.bind({})